//
//  MyFunctions.swift
//  Rich or Poor
//
//  Created by Martin Cordoba on 20/7/22.
//

import Foundation

/*
var stateValue = 0
var textContent = ["¡Hola Mundo! Presiona 'Continuar' cuando estés listo","¿Estás seguro?","¡Comenzamos!"]
var buttonText = ["Continuar", "¡Por supuesto!","All right!!! 😎"]

@IBOutlet weak var startButton: UIButton!
@IBOutlet weak var backButton: UIButton!
@IBOutlet weak var myText: UITextView!

myText.text = textContent[0]
myText.textAlignment = .center
startButton.sizeToFit()
backButton.sizeToFit()
backButton.isHidden = true
startButton.setTitle(buttonText[0], for: .normal)
backButton.setTitle("Atrás", for: .normal)


@IBAction func startButtonAction(_ sender: Any) {
    stateValue += 1
    toggleValues()
}

@IBAction func backButtonAction(_ sender: Any) {
    if stateValue > 0{
        stateValue -= 1
        toggleValues()
    }
    else{
        backButton.isHidden = true
    }
}

func toggleValues(){
    if stateValue > 2{
        myText.text = textContent[stateValue]
        startButton.setTitle(buttonText[stateValue], for: .normal)
        startButton.isHidden = true
        myText.isHidden = true
    }
    else if stateValue < 2 && stateValue > 0{
        startButton.isHidden = false
        myText.isHidden = false
        backButton.isHidden = false
        myText.text = textContent[stateValue]
        startButton.setTitle(buttonText[stateValue], for: .normal)
        
    }
    else{
        backButton.isHidden = true
    }
}
*/
